import { FC } from "react";
import { Container, ListContainer } from "./style";

const List: FC = () => {
  return (
    <Container>
      <ListContainer>products list</ListContainer>
    </Container>
  );
};

export default List;
