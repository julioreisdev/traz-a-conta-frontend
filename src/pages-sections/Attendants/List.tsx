import { FC } from "react";
import { Container, ListContainer } from "./style";

const List: FC = () => {
  return (
    <Container>
      <ListContainer>attendants list</ListContainer>
    </Container>
  );
};

export default List;
